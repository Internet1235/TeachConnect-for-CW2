[project]
name = "com-Internet123-TeachConnect"
version = "1.1.1"
description = "这是TeachConnect CW插件版"
authors = [
    {name = "Wakana_0"},
]
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["class-widgets-sdk"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
